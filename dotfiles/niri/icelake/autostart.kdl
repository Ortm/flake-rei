// Path is distro specific (Arch/KDE). Machines with a different polkit agent
// (e.g. /usr/libexec/polkit-gnome-authentication-agent-1) override it here.
spawn-sh-at-startup "/usr/lib/polkit-kde-authentication-agent-1"
